import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

const isto = document.getElementById("root");

const root = ReactDOM.createRoot(isto);

root.render(<App />);
